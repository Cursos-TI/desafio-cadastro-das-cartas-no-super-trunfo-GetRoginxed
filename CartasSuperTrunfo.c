#include <stdio.h>

int main() {

    char estado; // Letra de 'A' a 'H' para identificar o estado da cidade
    char codigo[2]; // Código da cidade, composto por 2 dígitos (inicia-se em '01',)
    char nome[50]; // Nome da cidade (nome único, sem espaços)
    int populacao; // População da cidade
    float area; // Área da cidade em km²
    float pib; // PIB da cidade em bilhões de reais
    int pontos_turisticos; // Número de pontos turísticos da cidade

    return 0;
}
